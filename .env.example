# Discord webhook URL for order/quote notifications
DISCORD_WEBHOOK_URL_ORDER=https://discord.com/api/webhooks/your_webhook_id/your_webhook_token

# Discord webhook URL for quote requests (falls back to DISCORD_WEBHOOK_URL_ORDER)
DISCORD_WEBHOOK_QUOTES=https://discord.com/api/webhooks/your_webhook_id/your_webhook_token

# Discord webhook URL for contact form submissions
DISCORD_WEBHOOK_CONTACT=https://discord.com/api/webhooks/your_webhook_id/your_webhook_token

# SendGrid API key for confirmation emails
SENDGRID_API_KEY=SG.your_sendgrid_api_key_here

# Admin email for fallback notifications when Discord is unavailable
ADMIN_EMAIL=your-admin-email@example.com

# Webhook HMAC secret for signature validation
# Generate with: openssl rand -hex 32
WEBHOOK_SECRET=your_webhook_secret_here

# (Optional) Comma-separated list of allowed origins for CORS
WEBHOOK_ALLOWED_ORIGINS=
